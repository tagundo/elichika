# the built server binary at the repo root (anchored so it does not also match
# the `elichika` directory in the Android app's Java package path)
/elichika
# the alisa maintenance-server binary (`go build ./webui/alisa/` drops it at the root)
/alisa
elichika_host

static/

.vscode/


backup/
raw_data/
local_git_server/
*.exe
*.bat
*.txt
*.db
*.backup
*.json
*.sql
*.dll
template.go

__pycache__/
*.pyc
